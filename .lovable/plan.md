

The user wants the `/download` page to keep working as it does today — clicking the button downloads the prebuilt `GlowHub.apk` from the GitHub Releases URL. They're asking "is there a way" — the answer is: **it already does exactly that**, no code change needed.

Let me confirm by checking the current Download page.
